import React from 'react';

const FormPainting = () => {
    return (
        <div>
            <h2 className="section-title">Nueva Pintura</h2>
            <p className="admin-subtitle">Detalles específicos para pinturas (técnica, tipo de lienzo, marco).</p>
            <div className="admin-form">
                {/* Inputs pendientes */}
            </div>
        </div>
    );
};

export default FormPainting;